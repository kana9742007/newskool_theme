$(document).ready(function(){
    $('.slider').slick({
        centerMode: true,
        centerPadding: 0,
        dots:true,
        dotsClass: 'slick-dots',
        focusOnSelect:true,
        autoplay:true,
        autoplaySpeed:5000,
        arrows: false,
        // customPaging:,
        // appendDots:,
    });
  });
